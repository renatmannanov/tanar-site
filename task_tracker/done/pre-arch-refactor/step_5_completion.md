# Шаг 5: Завершение плана

> Статус: pending

## Чеклист

- [ ] Все шаги (1–4) выполнены ([x] в PLAN.md), каждый — отдельным коммитом
- [ ] Критерии готовности из PLAN.md проверены командами:
  - [ ] `npm run typecheck` — чисто
  - [ ] `npm run lint` — чисто (включая новое no-restricted-imports)
  - [ ] `npm run build` — проходит
  - [ ] `npm run test:e2e` — все зелёные
- [ ] Внешнее поведение сайта не изменилось (главная: 4 плитки категорий; /catalog: 6 чипов; карточки и страница товара выглядят как раньше)
- [ ] Категории — единый источник `CATEGORIES` в `src/lib/product.ts`
- [ ] `Product.gradient` опционально, доступ через `getProductGradient`
- [ ] Пути картинок централизованы, публичные сигнатуры не изменены
- [ ] Прямой импорт `@/data/products` запрещён ESLint вне `src/lib/product.ts`
- [ ] Мусор убран (нет временных импортов для проверки eslint-правила, нет закомментированного кода)
- [ ] Статус в PLAN.md → done
- [ ] Папка перемещена: `todo/pre-arch-refactor/` → `done/pre-arch-refactor/`

## Заметка для Фазы 0 (БД-миграция)

Этот рефактор подготовил точки подмены:
- `@/lib/product` — единственный вход к данным → здесь подключится БД-репозиторий.
- `getProductGradient` / `productImagePath` / `getProductCardImage` / `getProductGalleryShots` — реализации заменятся на чтение из `MediaAsset`, вызовы в компонентах не тронутся.
- `CATEGORIES` — станет таблицей/справочником в БД.
- Переход на async произойдёт в Фазе 0 вместе с БД (НЕ в этом плане).
