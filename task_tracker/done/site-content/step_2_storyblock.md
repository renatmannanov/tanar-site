# Шаг 2: Обновить StoryBlock на главной

> Зависит от: шаг 1 (статья «О бренде» существует — на неё ссылка)
> Статус: [ ] pending

## Задача

`src/components/home/StoryBlock.tsx` — тизер «О бренде» на главной (секция `id="story"`, на неё ведёт «О бренде» из Header). Сейчас текст — старая выдумка с «встречающая рассвет».

1. **Обновить текст** под новый манифест из `internal/content-source/text.md` (блок 1). Оставить формат: заголовок + 2-3 абзаца (это тизер, не весь манифест). Заголовок можно оставить «Рождены в горах» или взять из манифеста — выбрать один (зафиксировать: **оставить «Рождены в горах»**, текст абзацев — сжатый манифест).
2. **Свести перевод**: если в тексте упоминается значение «Таңар» — использовать «тот, кто встречает рассвет» (канон из шага 1).
3. **Добавить ссылку** «Читать о бренде →» на `/blog/o-brende-tanar` (статья из шага 1). `next/link`, стиль — неброская текстовая ссылка под абзацами.
4. Картинку оставить как есть (`Placeholder` ХАН ТЕНГРИ) — фото бренда не в scope этого плана.

### Также: CLAUDE.md

В `CLAUDE.md` (корень) в шапке есть «(каз. "встречающая рассвет")». Поправить на «тот, кто встречает рассвет» — единый перевод по проекту.

## Тесты

- e2e `home.spec.ts` («all main sections are visible») проверяет секции главной — StoryBlock остаётся с `id="story"`, не ломается.
- Проверить, что ссылка на `/blog/o-brende-tanar` ведёт на существующую статью (200).

## Команды для верификации

```powershell
npm run typecheck
npm run lint
npm run build
npm run db:up; npm run db:seed
npm run test:e2e
```

## Критерии готовности

- [ ] StoryBlock: текст из нового манифеста, заголовок «Рождены в горах», перевод «тот, кто встречает рассвет»
- [ ] Ссылка «Читать о бренде →» на `/blog/o-brende-tanar` работает
- [ ] CLAUDE.md: перевод «Таңар» поправлен на единый
- [ ] `npm run typecheck/lint/build/test:e2e` зелёные
- [ ] Коммит: `content(home): refresh StoryBlock to real manifest + link to brand article`
