# Шаг 3: Страница /contacts (контент в коде)

> Зависит от: нет (но логически после шага 1-2)
> Статус: [ ] pending

## Задача

Новый публичный роут `/contacts` (`src/app/(public)/contacts/page.tsx`). Контент — **константа в коде** (итерация 1; перенос в БД — шаг 8). Под `(public)` layout (Header/Footer наследуются автоматически).

### Контент (из `internal/content-source/data-from-docx.md`, раздел 1)

- **Телефоны:** +7 701 744 38 73 (Айман), +7 707 722 05 06 (Милена) — кликабельные `tel:`.
- **Instagram:** @tanar_qazaqstan → https://instagram.com/tanar_qazaqstan (внешняя ссылка, `target="_blank" rel="noopener"`).
- **Город:** Алматы.
- **Адрес магазина:** ул. Розыбакиева, 205Д.
- **Самовывоз:** Да — ул. Розыбакиева, 205Д (маршрут уточнять по телефону).
- **Email:** НЕ показывать (нет у заказчика).
- **Telegram:** НЕ показывать (пока нет).

### Технические детали

- Серверный компонент, статика (контент в коде → не нужен `force-dynamic`). В итерации 2 станет dynamic при чтении из БД.
- Вынести контакты в константу `CONTACTS` в этом же файле или в `src/lib/site-contacts.ts` — зафиксировать: **константа в `src/lib/site-contacts.ts`** (client-safe, чистый объект; пригодится футеру в шаге 5 и переезду в БД в шаге 8 — единый источник уже сейчас).
- `metadata` (title/description) для SEO: «Контакты — Tanar».
- Заголовок страницы h1 «Контакты». Адрес/телефоны — семантично (`<address>` уместно).
- Реквизиты (ИП/БИН) на этой странице НЕ обязательны (идут в футер, шаг 5). Если добавлять — мелким внизу. Зафиксировать: **на /contacts реквизиты не дублируем**, только в футере.

## Тесты

- Новый e2e (или дополнить `layout.spec.ts`): `/contacts` → 200, виден h1 «Контакты», виден телефон/адрес.
- Проверить, что Header/Footer есть на странице (наследуются от `(public)` layout).

## Команды для верификации

```powershell
npm run typecheck
npm run lint
npm run build
npm run db:up; npm run db:seed
npm run test:e2e
```

## Критерии готовности

- [ ] `/contacts` отдаёт 200 под Header/Footer; h1 «Контакты»
- [ ] Видны телефоны (tel:), Instagram (внешняя), адрес, самовывоз; email/telegram отсутствуют
- [ ] Контакты вынесены в `src/lib/site-contacts.ts` (единый источник для футера)
- [ ] `metadata` задан
- [ ] `npm run typecheck/lint/build/test:e2e` зелёные
- [ ] Коммит: `feat(site): contacts page (static content)`
