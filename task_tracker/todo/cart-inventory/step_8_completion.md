# Шаг 8: Завершение плана

> Зависит от: шаги 1–7
> Статус: [ ] pending

## Чеклист

- [ ] Все шаги плана выполнены ([x] в PLAN.md)
- [ ] Критерии готовности из PLAN.md проверены (каждый — командой или e2e)
- [ ] SQL-сверка журнала: после прогона inventory-lifecycle в dev-БД
      `SELECT reason, delta FROM inventory_log ORDER BY created_at` содержит
      reservation/+, reservation_release/−, sale/− в ожидаемом порядке
- [ ] Smoke test вручную (dev): карточка с точкой наличия → распроданный размер →
      «Узнать о поступлении» → заказ → подтверждение в админке → размер распродан
      на витрине → отмена → размер вернулся
- [ ] Не сломано: `npm run test:e2e` полностью зелёный (все старые спеки)
- [ ] CLAUDE.md обновлён: «Данные» (механика резерва по статусам — реализована,
      пороги точек, inventory_log-семантика delta), «Модульная структура»
      (`core/inventory` больше не заглушка, два входа), «Структура сайта»
      (точки наличия, «Узнать о поступлении»)
- [ ] ARCHITECTURE-ecommerce.md: блок «Привязка к статусам заказа» помечен
      реализованным (ссылка на этот план); открытый вопрос №1 (момент резерва)
      закрыт — «резерв на подтверждении, зафиксировано 2026-06-11»
- [ ] context.md проекта обновлён (фокус, прогресс, решения)
- [ ] Мусор убран (временные файлы, console.log, закомментированный код)
- [ ] Заметка для прод-деплоя в progress.md → Learnings: миграций НЕТ,
      только код (`build --no-cache` + `up -d`); остатки на проде уже заведены —
      механика включается сразу, отдельных действий заказчицы не требуется
- [ ] Статус в PLAN.md → done
- [ ] Папка перемещена: `task_tracker/todo/cart-inventory/` → `task_tracker/done/cart-inventory/`

## Команды для верификации

```bash
npm run typecheck && npm run lint && npm run build
npm run test:e2e
```
